_format_version: "2.1"

services:
  - name: gist-service
    url: https://api.github.com
    routes:
      - name: gists-route
        paths:
          - /gists
  - name: server
    url: http://host.docker.internal:8002
    routes:
      - name: hello-route
        paths:
          - /hello

plugins:
  - name: rate-limiting
    service: server
    config:
      minute: 5
      policy: local